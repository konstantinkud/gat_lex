
gatling {
  data {
    writers = [console, file, graphite]

    graphite {
      host = "192.168.0.216"   # <-- IP твоей Linux-машины
      port = 2003
      protocol = "tcp"
      rootPathPrefix = "gatling"
      bufferSize = 8192
      writePeriod = 1          # пишем каждую секунду
    }
  }
}